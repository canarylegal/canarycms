GUI updater test payload
========================

Committed on GitHub main for end-to-end testing of Admin → Deploy and the post-login update prompt.
Safe to delete from the repository after you have verified the update flow.

The running backend image commit must be behind GitHub main for the prompt to appear.
